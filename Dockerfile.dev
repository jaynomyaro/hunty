FROM node:18-alpine

WORKDIR /app

# Copy package files and install pnpm
COPY package.json pnpm-lock.yaml ./
RUN npm install -g pnpm && pnpm install --frozen-lockfile

# Copy the rest of the source code
COPY . .

EXPOSE 3000

# Start the development server
CMD ["pnpm", "dev"]
